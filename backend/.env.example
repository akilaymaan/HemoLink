PORT=5000
NODE_ENV=development
# MongoDB: use your cloud connection URL (e.g. MongoDB Atlas)
MONGODB_URI=mongodb+srv://user:password@cluster.xxxxx.mongodb.net/hemolink
JWT_SECRET=your-super-secret-key-change-in-production

# ML service (Python FastAPI). Set to backend URL to use real ML/NLP; set to empty string to use rule-based only.
ML_SERVICE_URL=http://127.0.0.1:8000
# ML_SERVICE_TIMEOUT_MS=5000
